import { Routes } from '@angular/router';
import { JsonDataComponent } from './Components/json-data/json-data.component';

export const routes: Routes = [
    { path: 'posts', component: JsonDataComponent }, // 'about' route
];
